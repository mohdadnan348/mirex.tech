/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'i.pinimg.com', 't4.ftcdn.net', 'www.shutterstock.com', 'imageio.forbes.com'],
  },
};

export default nextConfig;
